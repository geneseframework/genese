export enum FactorCategory {

    AGGREGATION = 'aggregation',
    BASIC = 'basic',
    CONTEXT = 'context',
    DEPTH = 'depth',
    NESTING = 'nesting',
    RECURSION = 'recursion',
    STRUCTURAL = 'structural'

}
