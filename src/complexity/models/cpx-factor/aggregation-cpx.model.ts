export class AggregationCpx {

    arr ?= 0;                               // Array of arrays
    density ?= 0;                           // Accumulation of nodes on a same line of code
    logicDoorChange ?= 0;                   // a "or" after a "and" (or a "or" after a "and") without brackets
    logicDoorChangeWithBrackets ?= 0;       // a "or" after a "and" (or a "or" after a "and") without brackets

}
