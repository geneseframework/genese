import { TreeNode } from '../models/tree/tree-node.model';

/**
 * Interface of the objects corresponding to an AST node
 */
export interface HasTreeNode {

    treeNode?: TreeNode;     // The AST node of the object

}
